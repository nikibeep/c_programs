#include<stdio.h>
int main()
{int ch,arr[100],i;
    do{
        switch(ch){
            case 1: void readlist(){
                int ele=100;
                for(i=0;i<ele;i++)
                printf("elements:%d",a[i]);
            }
            case 2: void Sorted(){
                readlist();
                for(i=100;i>a[i];--i)
                return a[i];
            }
            case 3: void displaylist(){
                Sorter();
                print("Sorted array: ",a[i];)
            }
            case 4: void printSum(){
                Sorted();
                int j[100];
                for(int i=0;i<100;i++){
                    if(a[i]%2!=0 && a[i]%3!=0){
                        j[i]+=a[i];
                        printf("sum of elemnets:%d",j[i]);
                    }
                }
            }
            case 5:int indexofmin(int a,int b){
                sorter();
                for(int i=a;a<100;i++)
                for(int j=b;b>100;j++)
                print("elements: ",a[i]b[j]);
            }
        }
    }
    while(ch<6){
        int ch,arr[100],i;
    print("Enter the choice:");
    scanf("%d",ch);
}
}